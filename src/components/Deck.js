import React, { useState } from "react";
import SingleCard from './SingleCard';

const Deck = (props) => {
    const listImg = props.imgs;

    console.log(listImg)

    return (
        <SingleCard></SingleCard>
    )

}

export default Deck